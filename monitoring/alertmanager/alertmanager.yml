global:
  resolve_timeout: 5m

route:
  receiver: default-local
  group_by:
    - alertname
    - severity
    - service
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 2h

receivers:
  - name: default-local

templates: []
