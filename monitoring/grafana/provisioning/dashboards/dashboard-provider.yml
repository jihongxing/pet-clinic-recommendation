apiVersion: 1

providers:
  - name: petmed-dashboards
    orgId: 1
    folder: PetMed
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards/json
